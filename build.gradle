apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0'

jar {
    manifest {
        attributes 'Implementation-Title': 'DataFilter', 'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}